<h2>New book</h2>

    <%= form_with model: @book do |f| %>
    
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <div class="btn">
    <%= f.submit 'Create book' %>
    </div>
    <% end %>
